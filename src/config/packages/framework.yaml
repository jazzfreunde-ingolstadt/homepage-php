framework:
    secret: '%env(APP_SECRET)%'
    #csrf_protection: true
    http_method_override: false
    handle_all_throwables: true

when@prod:
  framework:
    error_controller: Jazzfreunde\App\Controller\LegacyContentController::show
