<div class="relative block w-full min-h-min z-0">
  <img class="absolute top-0 w-full h-full object-cover -z-10 {{ class|default('') }}" src="{{ image_url|raw }}" />
  <div class="relative float-left w-full">
    {% block infront %}

    {% endblock %}
  </div>
</div>
