<div class="relative block w-full min-h-min z-0 {{ class|default('') }}">
  <img class="absolute top-0 w-full h-full object-cover -z-10" src="{{ image_url|raw }}" />
  <div class="relative float-left w-full">
    {% block infront %}

    {% endblock %}
  </div>
</div>
